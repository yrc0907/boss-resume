{
  "name": "boss-job-helper",
  "private": true,
  "version": "0.1.0",
  "description": "Boss 直聘岗位筛选与投递准备 Chrome 扩展",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node scripts/test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
