{
  "name": "boss-job-helper",
  "private": true,
  "version": "0.1.0",
  "description": "Boss 直聘岗位筛选与投递准备 Chrome 扩展",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && pnpm test:api && node scripts/test.mjs",
    "test:api": "tsx scripts/test-api.ts"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
